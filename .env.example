# Google Gemini API Configuration
# Get your API key from: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your_google_api_key_here

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# ===== INVITE SYSTEM CONFIGURATION =====

# Enable/disable invite system
INVITE_LOG_ENABLED=true
INVITE_CONSOLE_LOG=true
INVITE_FILE_LOG=true
INVITE_STRICT_MODE=false

# Logging configuration
INVITE_LOG_DIR=./logs
INVITE_LOG_MAX_SIZE=10485760
INVITE_LOG_RETENTION_DAYS=30

# Invite keys (JSON format)
# Format: {"username1": "key1", "username2": "key2"}
INVITE_KEYS='{"admin": "admin-secret-key-2024", "demo": "demo-access-key", "premium": "premium-user-key"}'

# Cookie domain for production (optional)
# Set this to your domain when deploying to production
# Example: COOKIE_DOMAIN=.yourdomain.com
COOKIE_DOMAIN=

# CORS Configuration for production
# Allowed origins (comma-separated)
# Example: CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=

# Wildcard domains (comma-separated)
# Example: CORS_WILDCARD_DOMAINS=yourdomain.com,anotherdomain.com
CORS_WILDCARD_DOMAINS=

# Protected API routes (comma-separated)
# Default: /api/askgemini,/api/ocr/process,/api/auth/verify-invite
CORS_PROTECTED_ROUTES=

# Wallpaper Configuration
# URL for background wallpaper image/gif on landing page
WALLPAPER_URL=https://example.com/your-wallpaper.gif