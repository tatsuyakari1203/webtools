services:
  webtools:
    image: tatsuyakari/webtools:latest
    container_name: webtools-frontend-app
    restart: unless-stopped
    # ports không cần thiết khi sử dụng network_mode: host
    # ports:
    #   - "5005:3000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - NEXT_TELEMETRY_DISABLED=1
      - BACKEND_URL=http://172.17.0.1:7777
      - NEXT_PUBLIC_BACKEND_URL=http://172.17.0.1:7777
      - PORT=5005
    network_mode: "host"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5005"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # depends_on:
      # - webtools-backend

  # webtools-backend service removed - using local Python server instead

networks:
  webtools-network:
    driver: bridge

# Uncomment nếu cần persistent volumes
# volumes:
#   webtools-data:
#     driver: local